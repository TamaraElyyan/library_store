spring.application.name=LibraryStore
# Trick to let IntelliJ view endpoints through actuator.
management.endpoints.jmx.exposure.include=*

# Database connection & configuration
spring.datasource.username=sa
spring.datasource.password=Tamara@8
spring.datasource.url=jdbc:sqlserver://localhost;databaseName=Library_ecommerce;encrypt=true;trustServerCertificate=true

spring.jpa.hibernate.ddl-auto=update

# Encryption configuration
encryption.salt.rounds=10

# JWT configuration
jwt.algorithm.key=SuperSecureSecretKey
jwt.issuer=eCommerce
jwt.expiryInSeconds=604800